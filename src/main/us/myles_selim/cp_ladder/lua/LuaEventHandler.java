package us.myles_selim.cp_ladder.lua;

import us.myles_selim.cp_ladder.EventListener;

public class LuaEventHandler implements EventListener {

	// private boolean handle = true;

	// TODO: fix this
	// @EventSubscriber
	// public void onDiscordEvent(GuildEvent event) {
	// if (event.getGuild() == null
	// || event.getGuild().getId().asLong() == StarotaConstants.SUPPORT_SERVER)
	// return;
	// // if (event instanceof MessageReceivedEvent)
	// // if (((MessageReceivedEvent)
	// // event).getMessage().getContent().equals("toggle"))
	// // handle = !handle;
	// // if (!handle)
	// // return;
	// // DiscordEventLib.handleEvent(event);
	// LuaUtils.handleEvent(event);
	// }

}
